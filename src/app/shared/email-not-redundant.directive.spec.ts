import { EmailNotRedundantValidatorDirective } from './email-not-redundant.directive';

describe('EmailNotRedundantDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailNotRedundantValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
