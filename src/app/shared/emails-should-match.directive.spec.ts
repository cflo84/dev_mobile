import { EmailsShouldMatchDirective } from './emails-should-match.directive';

describe('EmailsShouldMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailsShouldMatchDirective();
    expect(directive).toBeTruthy();
  });
});
