import { PasswordsShouldMatchDirective } from './passwords-should-match.directive';

describe('PasswordsShouldMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordsShouldMatchDirective();
    expect(directive).toBeTruthy();
  });
});
